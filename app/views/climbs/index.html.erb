<%- model_class = Climb.new.class -%>

<%= render 'climbs/climb_list', :climbs => @climbs %>

<% if can? :create, Climb %>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_climb_path,
            :class => 'btn btn-primary' %>
<% end %>
