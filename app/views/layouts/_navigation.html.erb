<!-- .btn-navbar is used as the toggle for collapsed navbar content -->
<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
         <span class="icon-bar"></span>
         <span class="icon-bar"></span>
         <span class="icon-bar"></span>
       </a>

<%= link_to "Hard Climbs", root_path, :class => 'brand' %>
<div class="nav-collapse collapse" id="main-menu">
  <ul class="nav" role="navigation">
   <li class="dropdown">
     <%= link_to "Ascents", ascents_path, :role => "button"%>
   </li>
   <li>
    <%= link_to "Climbers", climbers_path, :role => "button"%>
   </li>
   <li class="dropdown">
     <a href="#" id="drop3" role="button" class="dropdown-toggle" data-toggle="dropdown">Climbs <b class='caret'></b></a>
     <ul class="dropdown-menu" role="menu" aria-labelledby="drop3">
       <li>
         <%= link_to "All Styles", climbs_path, :tabindex => '-1' %>
       </li>
       <% Style.all.each do |s| %>
        <li>
          <%= link_to s.name, climbs_path(:styles => s.id) %>
        </li>
      <% end %>
     </ul>
   </li>
   <li>
     <%= link_to "Areas", areas_path, :role => "button" %>
  </li>
  <li>
  	<%= link_to "Media", medias_path, :role => "button" %>
   </li>
  </ul>
  <%= form_tag search_path,
   method: :get, :class => "navbar-search pull-right" do %>
    <%= text_field_tag :query, params[:query], :class => "search-query span2", :placeholder => "Search" %>
  <% end %>

  
</div>