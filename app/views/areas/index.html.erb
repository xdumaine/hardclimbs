<%- model_class = Area.new.class -%>
<% if can? :create, Area %>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_area_path,
            :class => 'btn btn-primary' %>
<% end %>
<%= render 'areas/area_list', :areas => @areas %>