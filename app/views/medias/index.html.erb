<%- model_class = Media.new.class -%>
<% if can? :create, Media %>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_media_path,
            :class => 'btn btn-primary' %>
<% end %>
<%= render 'medias/media_list', :medias => @medias %>