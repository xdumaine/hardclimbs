<%- model_class = Climber.new.class -%>
<% if can? :create, Climber %>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_climber_path,
            :class => 'btn btn-primary' %>
<% end %>
<%= render 'climbers/climber_list', :climbers => @climbers %>