/*$(function(){
	
	$('#world-map').vectorMap({
	  map: 'world_mill_en',
	  series: {
	    regions: [{
	      values: gon.area_data,
	      scale: ['#F28746', '#e36b23'],
	      normalizeFunction: 'polynomial'
	    }]
	  },
	  onRegionLabelShow: function(e, label, code){
		console.log(label.html()+' (Climbs - '+ gon.area_data[code]+')');
	    label.html(label.html()+' (Climbs - '+ gon.area_data[code]+')');
	  }
	});
});*/